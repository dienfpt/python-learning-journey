# phase4_fastapi

> Stub — điền notes khi học tới phase này.
> Tạo thêm file .py tương ứng trong examples/phase4_fastapi/ theo template ở README.

## Nội dung dự kiến

- Pydantic v2: validation, serialization
- Dependency Injection (Depends) — so sánh NestJS DI
- Routing, request/response models, middleware, error handling
- Async DB: SQLAlchemy 2.0 async + asyncpg / SQLModel
- Background tasks, WebSocket
